services:
  app:
    build: .
    ports:
      - "8000:80"
    environment:
      - APP_ENV=local
      - APP_KEY=base64:yrMIiOihTD/wCN5hbakEGedqTpoZfvESrQxg7Ib/TO0=
      - APP_DEBUG=true
      - APP_URL=http://localhost:8000
      - DB_CONNECTION=sqlite
      - DB_DATABASE=/var/www/html/database/database.sqlite
      - IAE_SSO_URL=${IAE_SSO_URL}
      - IAE_API_KEY=${IAE_API_KEY}
      - IAE_TEAM_ID=${IAE_TEAM_ID}
      - IAE_NIM=${IAE_NIM}
      - SESSION_DRIVER=file
      - CACHE_STORE=file

volumes: {}

